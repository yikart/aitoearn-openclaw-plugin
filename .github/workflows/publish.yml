name: Publish to npm

on:
  release:
    types: [created]
  workflow_dispatch:

concurrency:
  group: publish-npm
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Verify version alignment
        run: npm run verify:versions

      - name: Run tests
        run: npm test

      - name: Build packages
        run: npm run build

      - name: Verify publish tarballs
        run: npm run pack:check

      - name: Smoke test installer CLI bin
        run: npm run smoke:installer-cli

      - name: Smoke test runtime package install
        run: npm run smoke:runtime-package

      - name: Resolve package versions
        id: versions
        run: |
          runtime_version=$(node -p "require('./packages/runtime/package.json').version")
          installer_version=$(node -p "require('./packages/installer/package.json').version")
          echo "runtime_version=$runtime_version" >> "$GITHUB_OUTPUT"
          echo "installer_version=$installer_version" >> "$GITHUB_OUTPUT"

      - name: Check runtime package publish state
        id: runtime_publish_state
        run: |
          if npm view "@aitoearn/openclaw-plugin@${{ steps.versions.outputs.runtime_version }}" version >/dev/null 2>&1; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Check installer package publish state
        id: installer_publish_state
        run: |
          if npm view "@aitoearn/openclaw-plugin-cli@${{ steps.versions.outputs.installer_version }}" version >/dev/null 2>&1; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish installer package
        if: steps.installer_publish_state.outputs.should_publish == 'true'
        run: npm publish --workspace @aitoearn/openclaw-plugin-cli --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish runtime package
        if: steps.runtime_publish_state.outputs.should_publish == 'true'
        run: npm publish --workspace @aitoearn/openclaw-plugin --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Report skipped packages
        run: |
          if [ "${{ steps.runtime_publish_state.outputs.should_publish }}" != "true" ]; then
            echo "Runtime package @aitoearn/openclaw-plugin@${{ steps.versions.outputs.runtime_version }} is already published; skipping."
          fi
          if [ "${{ steps.installer_publish_state.outputs.should_publish }}" != "true" ]; then
            echo "Installer package @aitoearn/openclaw-plugin-cli@${{ steps.versions.outputs.installer_version }} is already published; skipping."
          fi
